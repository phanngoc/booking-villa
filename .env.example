# Gmail configuration for email verification
# 1. Enable 2-Step Verification in your Google Account
# 2. Generate an App Password:
#    - Go to Google Account settings
#    - Search for "App Passwords"
#    - Select "Mail" and your device
#    - Copy the generated 16-character password
# 3. Use your Gmail address and the generated App Password below

GMAIL_USERNAME=
GMAIL_PASSWORD=

# Note: Do not use your regular Gmail password. 
# Always use App Password for security reasons.
# Remember to add .env to .gitignore to keep your credentials safe. 

# Gmail SMTP Settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587  # Sử dụng port 587 cho TLS
SMTP_PORT_SSL=465  # Port thay thế sử dụng SSL
SMTP_PROTOCOL=TLS
SMTP_DOMAIN=gmail.com
SMTP_AUTH=true
SMTP_STARTTLS_AUTO=true

# Giới hạn gửi email
SMTP_SENDING_LIMIT=100  # Giới hạn số người nhận mỗi ngày cho G Suite

# Google OAuth
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret

